name: Build and Release

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            platform_name: Windows
          - os: ubuntu-latest
            platform_name: Linux
          - os: macos-latest
            platform_name: macOS

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Setup ccache
      if: matrix.os != 'windows-latest'
      uses: hendrikmuhs/ccache-action@v1.2
      with:
        key: ${{ runner.os }}-ccache-${{ github.sha }}
        restore-keys: ${{ runner.os }}-ccache-

    - name: Set up .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install Qt
      uses: jurplel/install-qt-action@v3
      with:
        version: '5.15.2'
        target: 'desktop'
        arch: ${{ matrix.os == 'windows-latest' && 'win64_msvc2019_64' || '' }}
        install-deps: 'true'
        modules: 'qtcharts qtwebengine'
        cache: 'true'

    - name: Setup MSVC (Windows)
      if: matrix.os == 'windows-latest'
      uses: ilammy/msvc-dev-cmd@v1

    # --- Сборка CppGen (C#) ---
    
    - name: Build CppGen
      shell: bash
      run: |
        # Патчим версию .NET на 8.0 (работает везде)
        python -c "p='CppGen/CppGen/CppGen.csproj'; d=open(p).read(); open(p,'w').write(d.replace('net6.0','net8.0'))"
        
        # Собираем в папку BuildGen в корне репозитория
        dotnet build CppGen/CppGen/CppGen.csproj -c Release -o ./BuildGen

    - name: Run CppGen
      shell: bash
      run: |
        # 1. ЯВНО копируем файл конфигурации в папку запуска
        # Берем из CppGen/CppGen/gml.json (где ты его оставил)
        cp CppGen/CppGen/gml.json ./BuildGen/gml.json

        # Заходим в папку с программой
        cd BuildGen
        
        # 2. ФИКС ДЛЯ MAC/LINUX (Case Sensitivity)
        # Создаем копии с разными именами, чтобы система нашла файл при любом написании в коде
        cp gml.json GML.json
        cp gml.json Gml.json
        
        # 3. ЗАПУСК С АБСОЛЮТНЫМИ ПУТЯМИ
        # ${{ github.workspace }} - это полный путь (D:\a\Mine-imator\...)
        # Это решает проблему "No GameMaker project found"
        dotnet CppGen.dll -game "${{ github.workspace }}/GmProject" -cpp "${{ github.workspace }}/CppProject"

    # --- Сборка проекта (C++) ---

    - name: Configure CMake
      run: |
        if [ "${{ matrix.os }}" != "windows-latest" ]; then
          export CC="ccache gcc"
          export CXX="ccache g++"
          if [ "${{ matrix.os }}" == "macos-latest" ]; then
             export CC="ccache clang"
             export CXX="ccache clang++"
          fi
        fi
        cmake -S CppProject -B build -DCMAKE_BUILD_TYPE=Release

    - name: Build with CMake
      run: |
        cmake --build build --config Release

    # --- Упаковка ---

    - name: Package (Windows)
      if: matrix.os == 'windows-latest'
      shell: cmd
      run: |
        mkdir release_dist
        copy build\Release\*.exe release_dist\
        xcopy /E /I GmProject\datafiles\Data release_dist\Data
        windeployqt release_dist\*.exe --release --no-translations --no-system-d3d-compiler --no-compiler-runtime --no-opengl-sw

    - name: Package (Unix - Mac/Linux)
      if: matrix.os != 'windows-latest'
      shell: bash
      run: |
        mkdir release_dist
        find build -type f -perm +111 -not -name ".*" -exec cp {} release_dist/ \; || find build -type f -perm /111 -not -name ".*" -exec cp {} release_dist/ \;
        if [ -d "GmProject/datafiles/Data" ]; then
          cp -r GmProject/datafiles/Data release_dist/
        fi

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Mine-imator-${{ matrix.platform_name }}
        path: release_dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/heads/')
    permissions:
      contents: write
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Zip Artifacts
        run: |
          cd artifacts
          for dir in */; do
              platform=${dir%*/}
              zip -r "../$platform.zip" "$platform"
          done

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ github.sha }}
          name: Build ${{ github.sha }}
          files: artifacts/*.zip
          draft: false
          prerelease: false